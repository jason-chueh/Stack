package com.example.stack.home

import androidx.lifecycle.ViewModel
import com.example.stack.repository.StackRepository

class HomeViewModel(private val stackRepository: StackRepository) : ViewModel() {

}